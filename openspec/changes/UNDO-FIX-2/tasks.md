# Tasks: UNDO-FIX-2

- [x] T-1: Wire courseSelection mutations to Action Log (`selection:*` actions + snapshots + payload metadata).
- [x] T-2: Teach ActionLogPanel + i18n locales to describe the new entries (and populate missing solver-related labels).
- [x] T-3: Document change (apply.md), update PLAN/memory if needed, and run `npm run check`.
