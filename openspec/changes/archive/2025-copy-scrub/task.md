# Tasks
- [x] T1: 使用 `rg` 搜索 “热门/火爆/智能推荐”等关键词，列出命中清单。
- [x] T2: 删除 CourseCard & catalog 中 `hot` 状态，统一仅显示 `limited/full`。
- [x] T3: 更新 `openspec/specs/ui-course-cards/spec.md`，写明禁止使用模糊标签并说明 dataset 例外。
- [x] T4: 复检 `rg` 结果，确认 UI 文案已清理；记录 dataset 无法变更的课程名。
